
char** ln_to_fields(FILE *file, int ln_num) {
    //TODO: read line number `ln_num` until \n and return *char[] of all fields separated, if reached eof return NULL
    //TODO temp: run with a loop until ln_nm
    //split the line to fields with strtok
    //return the pointer to the first field
}